#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../localtunnel/node_modules/window-size/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../localtunnel/node_modules/window-size/cli.js" "$@"
  ret=$?
fi
exit $ret
